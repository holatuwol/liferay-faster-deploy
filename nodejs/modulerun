#!/bin/bash

modulerun() {
	pushd modules

	cat ${cache_home}/cachenpm.txt | dirnames | cut -d'/' -f 2- | tr '/' ':' | awk '{ print ":" $1 ":npmInstall" }' | xargs $(dirname "${BASH_SOURCE[0]}")/../gw

	popd > /dev/null
}

modulerun